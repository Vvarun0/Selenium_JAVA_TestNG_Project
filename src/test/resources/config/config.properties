# Configuration properties for Selenium project

baseUrl=https://www.google.com

browser=chrome

# Example: Credentials (for a real app, do not hardcode sensitive info directly here,
# consider environment variables or secure vault)
# username=testuser
# password=testpass
